steps:
  - name: 'gcr.io/cloudbuild/docker'
    args: ['build', '-t', 'my-backend-image', '-f', 'backend/Dockerfile', '.']
  - name: 'gcr.io/cloudbuild/docker'
    args: ['push', 'my-backend-image']

options:
  logging:
    defaultLogsBucketBehavior: CLOUD_LOGGING_ONLY
